import Camera from "../camera.js";
import { FontClass } from "../loaders/font_loader.js";
export declare function createLabelsLayer(font: FontClass): (context: CanvasRenderingContext2D, camera: Camera) => void;
