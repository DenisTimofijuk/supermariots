import Level from "./level";
import Camera from "./camera";
import Entity from "./entity";
export default function initDebugger(level: Level, camera: Camera, canvas: HTMLCanvasElement, mario: Entity, enable?: boolean): void;
